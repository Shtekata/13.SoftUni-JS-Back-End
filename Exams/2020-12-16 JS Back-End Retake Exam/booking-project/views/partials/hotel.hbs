{{#if isAuth}}
<a href="/hotels/details/{{_id}}" class="added-hotel">
    {{else}}
    <a href="/auth/login" class="added-hotel">
        {{/if}}
        <img src="{{imageUrl}}" alt="hotel-picture" class="picture-added-hotel">
        <h3>{{name}}</h3>
        <span>Free rooms: {{freeRooms}}</span>
    </a>